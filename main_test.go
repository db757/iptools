package main

import "testing"

func TestMain(t *testing.T) {
	t.Run("dummy test to help neotest-go discover package tests", func(t *testing.T) {
		// This is just a placeholder test to help neotest-go discover tests in subdirectories
		// You can remove this file if adding the neotest-go configuration solved the issue
	})
}
